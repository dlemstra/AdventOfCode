extern crate main;
use main::*;

fn main() {
    let (part1, part2) = wizard_simulator_20xx(51, 9);
    println!("Least amount of mana: {0}", part1);
    println!("Least amount of mana (hard mode): {0}", part2);
}
